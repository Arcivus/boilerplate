---
to: ../../js/app/<%= h.inflection.camelize(name, true) %>/duck/index.ts
---
import { combineReducers } from "redux";
// import reducerName from './reducers/reducer';

export default combineReducers({
  // statePart: reducerName
});
